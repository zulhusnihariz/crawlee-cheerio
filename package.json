{
  "name": "my-crawler",
  "version": "0.0.1",
  "description": "This is an example of a Crawlee project.",
  "dependencies": {
    "cors": "^2.8.5",
    "crawlee": "^3.0.0",
    "express": "^4.18.2",
    "node-cron": "^3.0.2",
    "sqlite": "^5.0.1",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "@apify/tsconfig": "^0.1.0",
    "@types/cors": "^2.8.15",
    "@types/express": "^4.17.20",
    "@types/node": "^18.0.0",
    "@types/node-cron": "^3.0.10",
    "@types/sqlite3": "^3.1.10",
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1",
    "prettier": "^3.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "split": "node --no-warnings=ExperimentalWarning --loader ts-node/esm/transpile-only src/split.ts",
    "combine": "node --no-warnings=ExperimentalWarning --loader ts-node/esm/transpile-only src/combine.ts",
    "dev": "nodemon --ignore ./storage/ index.ts",
    "start": "npm run start:dev",
    "start:prod": "node dist/main.js",
    "start:dev": "node --no-warnings=ExperimentalWarning --loader ts-node/esm/transpile-only src/scrape.ts",
    "build": "tsc",
    "test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1"
  },
  "author": "It's not you it's me",
  "license": "ISC"
}
